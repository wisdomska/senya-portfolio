# dependencies
node_modules/

# build output
dist/
.astro/

# environment — .env.example is the only env file that is committed
.env
.env.*
!.env.example

# platform
.vercel/
.lighthouseci/

# Generated share cards are committed — CI does not have the licensed font
# originals needed to rebuild them. Regenerate locally with `npm run og`.

# OS + editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Licensed font originals. The subset .woff2 files in public/fonts are
# committed (they are what the site serves); the full desktop faces are not.
src-fonts/

# Local dev-server config for the Claude Code browser pane.
.claude/
